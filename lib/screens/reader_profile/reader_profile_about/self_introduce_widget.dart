import 'package:flutter/material.dart';

class SelfIntroduceWidget extends StatelessWidget {
  const SelfIntroduceWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // show video introduce here
      child: Column(
        children: [
          const Text('Self Introduce'),
        ],
      ),
    );
  }
}
